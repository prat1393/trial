<?xml version="1.0" encoding="UTF-8" ?>

<authentication-providers>

	<!--
	authentication-provider
	===========================================================================
	name                - The label for this authentication provider
	type                - The kind of system the provider is for.  Valid
	                      case-sensitve values are ActiveDirectory, LDAP,
	                      Legacy, Custom and Embedded
	enabled             - Controls whether this authentication provider is
	                      available for authenticating users
	autoAddUsers        - Controls whether successfully authenticated users are
	                      automatically added to the ACL database.  When enabled,
	                      DevTest administrators will need to manually remove
	                      users from the DevTest database as users are removed
	                      from the system.
	defaultRole         - The role to assign to new users upon successful log
	                      in if they have no other roles assigned
	rejectUnmappedUsers - Prevent users with no ldap groups mapped to DevTest
	                      roles from logging in
	authenticateOnly    - Controls whether the authentication provider is only
	                      used for authenticating users while their respective
	                      accounts are loaded from the ACL database and not
	                      generated from LDAP group to role mappings
	url                 - The url of the server
	user-dn             - The distinguished name of the ldap user used to
	                      connect to the server
	user-password       - The password of the ldap user used to connect to the
	                      server
	user-dn-pattern     - A pattern which will be used to generate a distinguished
	                      name for the LDAP/AD user used to bind to the server.
	                      This element may be specified 0 or more times and the
	                      patterns will be tried in the order in which they occur
	                      in the <authentication-provider> element.  The pattern
	                      argument {0} will contain the username. An example would
	                      be "cn={0},ou=users,dc=example,dc=com".
	user-search-base    - The relative name to searching for uses
	user-search-filter  - The LDAP filter used to find user entries
	group-search-base   - The relative name to start searching for groups
	group-search-filter - The LDAP filter used to find group entries
	factory-class       - The fully qualified name of a java class that
	                      implements the com.ca.dts.security.authentication.AuthenticationProviderFactory
	                      interface.

	Example Authentication Providers:

	<authentication-provider
		name="Corp. Active Directory Server"
		autoAddUsers="false"
		authenticateOnly="false"
		enabled="true"
		type="ActiveDirectory"
		defaultRole="SV Power"
		rejectUnmappedUsers="true>
		<url>ldaps://server.example.com:3269</url>
		<user-dn>cn=readOnlyUser,ou=users,dc=example,dc=com</user-dn>
		<user-password>drowssap</user-password>
		<user-dn-pattern>cn={0},ou=users,dc=example,dc=com</user-dn-pattern>
		<user-search-base>dc=example,dc=com</user-search-base>
		<user-search-filter>(objectClass=user)</user-search-filter>
		<group-search-base>ou=groups</group-search-base>
		<group-search-filter>(member={0})</group-search-filter>
	</authentication-provider>

	<authentication-provider
		name="DevTest ACL Database"
		type="Embedded"
		enabled="true"/>

	<authentication-provider
		name="ITKO Authentication Module"
		type="Legacy"
		enabled="true"
		defaultRole="Guest"/>
		
	<authentication-provider
		name="My Authentication Provider"
		type="Custom"
		enabled="true"
		defaultRole="Guest"
		factory-class="com.example.FooAuthenticationProviderfactory">
		<property1>value1</property1>
		<property2>value2</property2>
	</authentication-provider>
	-->

	<authentication-provider
		name="ITKO Authentication Module"
		type="Legacy"
		enabled="true"
		defaultRole="Guest"/>

</authentication-providers>
