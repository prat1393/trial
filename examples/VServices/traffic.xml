<?xml version="1.0" ?>

<!-- This XML document represnets a skeleton or template that may be used -->
<!-- as a guide for creating an XML document to represent a list of       -->
<!-- sessions, each of which is a list of transactions.  Documents like   -->
<!-- this may be used to merge "raw" session data into LISA's SVSE        -->
<!-- image model.                                                         -->

<!-- The root element for this type of XML document must be traffic.  It  -->
<!-- accepts no attributes.                                               -->
<traffic>
    <!-- Each session is represented by a session element.  If a session  -->
    <!-- is instance-based, then no attributes should be specified.  Uf a -->
    <!-- session is token-based, then the string generation pattern that  -->
    <!-- can be used to create the ultimate conversation's session token  -->
    <!-- must be specified with the tokenPattern attribute.  If a token   -->
    <!-- pattern is specified and the transactions in the session are     -->
    <!-- merged into an existing conversation in the service image, then  -->
    <!-- the merged conversation's token pattern will be merged with the  -->
    <!-- one specified in the tokenPattern attribute.                     -->
    <session tokenPattern="token-generation-pattern">
        <!-- Under each session element must be a list of only            -->
        <!-- transaction elements.  Transactions are defined the same as  -->
        <!-- in a service image export XML file except that the children  -->
        <!-- and specifics child elements are not allowed.  No attributes -->
        <!-- may be specified.                                            -->
        <transaction>
            <!-- Under each transaction element must be one request       -->
            <!-- element and one responses element with at least one      -->
            <!-- response element under that.                             -->
            <request operation="op-name" matchTolerance="EXACT">
                <!-- The operation attribute is required.                 -->
                <!-- The match tolerance attribute is requred and must be -->
                <!-- one of EXACT, SIGNATURE or OPERATION.                -->
                <!-- There may be zero or one each of the arguments,      -->
                <!-- attributes and metaData elements as children of the  -->
                <!-- request element.  None are allowed to have           -->
                <!-- attributes.                                          -->
                <arguments>
                    <!-- Under each of these may be zero or more          -->
                    <!-- parameter elements.                              -->
                    <parameter name="[arg|attr|meta]-name">[arg|attr|meta]-value</parameter>
                    <!-- A type attribute is optional for parameter ele-  -->
                    <!-- ments under argument or attribute elements and   -->
                    <!-- not allowed for ones under a meta data element.  -->
                    <!-- If specified, the value of the type attribute    -->
                    <!-- must be "attr:<name-in-session>" for parameters  -->
                    <!-- under an attribute element.  The name in session -->
                    <!-- text represents the name under which this attri- -->
                    <!-- bute's value will be stored in a conversational  -->
                    <!-- session.                                         -->
                    <!-- If specified for parameters under an arguments   -->
                    <!-- element, the value of the type attribute must be -->
                    <!-- "arg:op;ms[;name-in-session[;date-pattern]]".    -->
                    <!-- where "op" must be one of ANY, EQUALS,           -->
                    <!-- NOT_EQUALS, LESS_THAN, LESS_THAN_OR_EQUALS,      -->
                    <!-- GREATER_THAN, GREATER_THAN_OR_EQUALS, REGEX, or  -->
                    <!-- PROPERTY_EXPRESSION, "ms" must be "true" or      -->
                    <!-- "false" and indicates whether this argument has  -->
                    <!-- been magic stringed, "name-in-session" is the    -->
                    <!-- same as for attributes and "date-pattern" is the -->
                    <!-- pattern of date the argument is expected to be   -->
                    <!-- in.                                              -->
                </arguments>
                <attributes>
                    <!-- (see arguments description above)                -->
                </attributes>
                <metaData>
                    <!-- (see arguments description above)                -->
                </metaData>
            </request>
            <responses>
                <!-- No attributes are allowed on the responses element.  -->
                <response thinkTime="0" binary="true">
                    <!-- The think time attribute specifies the think     -->
                    <!-- time for the response.  It is not required and   -->
                    <!-- defaults to null (interpreted as no think time). -->
                    <!-- The binary attribute must be "true" or "false"   -->
                    <!-- and indicates whether the body of the response   -->
                    <!-- is binary or text.  This defaults to false.      -->
                    <!-- There may be zero or one body element as a child -->
                    <!-- of the response element.  If the binary          -->
                    <!-- is true, then the value of the body element must -->
                    <!-- be a base-64 encoded representation of the body. -->
                    <!-- The body element accepts no attributes.          -->
                    <body>response-body-value</body>
                    <!-- There may be zero or one metaData element as a   -->
                    <!-- child of the response element.  If present, the  -->
                    <!-- metaData element must follow the same definition -->
                    <!-- as for the metaData element for a request.       -->
                    <metaData>
                        <!-- (see description for metaData elements for   -->
                        <!-- requests above)                              -->
                    </metaData>
                </response>
            </responses>
        </transaction>
        <!-- See the other XML files in this directory for examples of    -->
        <!-- how to define transactions.                                  -->
    </session>

    <!-- If there are stateless transactions to merge, then you'll need   -->
    <!-- a stateless element as well.  This element must be the last      -->
    <!-- element under the mergeData element.                             -->
    <stateless>
        <!-- Under stateless must be a list of only transaction elements. -->
        <!-- These have the same definition as the transaction elements   -->
        <!-- under the session element.                                   -->
        <transaction ... >
        </transaction>
        ...
    </stateless>
</traffic>
